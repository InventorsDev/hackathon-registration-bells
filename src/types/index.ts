export interface Student {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    course: string;
    enrollmentDate: string;
}

export interface Admin {
    id: string;
    username: string;
    email: string;
} 